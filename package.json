{
  "name": "felix-gluer-wf",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build:js": "babel src --out-dir dist --extensions '.js' && uglifyjs dist/*.js --compress --mangle -o dist/bundle.min.js",
    "build:css": "cleancss -o dist/styles.min.css src/*.css",
    "build": "npm run clean && npm run build:js && npm run build:css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@babel/cli": "^7.25.6",
    "@babel/core": "^7.25.2",
    "@babel/preset-env": "^7.25.4",
    "clean-css-cli": "^5.6.3",
    "uglify-js": "^3.19.3"
  },
  "dependencies": {
    "rimraf": "^6.0.1"
  }
}
